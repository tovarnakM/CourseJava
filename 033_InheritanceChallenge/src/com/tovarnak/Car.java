package com.tovarnak;

public class Car extends Vehicle {

    public Car(int velocity, String direction, int acceleration, String name) {
        super(velocity, direction, acceleration, name);
    }
}
